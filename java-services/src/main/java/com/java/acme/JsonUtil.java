package com.java.acme;

import org.bson.Document;

public class JsonUtil {

    public static void parseTicketInfo(Document ticketInfo) {

        try {
            Thread.sleep(50);
        } catch (InterruptedException ex) {
            Thread.currentThread().interrupt();
        }
    }

    public static void parseLoginInfo(Document loginInfo) {

        try {
            Thread.sleep(50);
        } catch (InterruptedException ex) {
            Thread.currentThread().interrupt();
        }
    }

    public static void parseFlightInfo(Document flightInfo) {

        try {
            Thread.sleep(50);
        } catch (InterruptedException ex) {
            Thread.currentThread().interrupt();
        }
    }

}
